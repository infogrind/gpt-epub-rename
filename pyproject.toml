[project]
name = "gpt-epub-rename"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "anthropic>=0.116.0",
    "openai>=2.8.1",
]

[project.scripts]
gpt-epub-rename = "rename:main"
